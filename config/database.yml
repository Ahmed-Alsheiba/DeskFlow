default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: deskflow_development

test:
  <<: *default
  database: deskflow_test

production:
  primary: &primary_production
    <<: *default
    host: <%= ENV["DB_HOST"] %>
    port: <%= ENV["DB_PORT"] %>
    username: <%= ENV["POSTGRES_USER"] %>
    password: <%= ENV["POSTGRES_PASSWORD"] %>
    database: deskflow_production
  cache:
    <<: *primary_production
    database: deskflow_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: deskflow_production_queue
    migrations_paths: db/queue_migrate